# UnionSelectPayloadGenerator
This is a simple generator for enumerating data types in UNION SELECT NULL,NULL -- based SQLi attack

# Usage
`python3 generator.py` - And just follow the instructions on the screen
